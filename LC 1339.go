/**
 * Definition for a binary tree node.
 * type TreeNode struct {
 *     Val int
 *     Left *TreeNode
 *     Right *TreeNode
 * }
 */

const MOD = 1e9 + 7
…        return root.Val + l + r
    }

    dfs(root)
    dfs2(root)

    return int(ans % MOD)
}